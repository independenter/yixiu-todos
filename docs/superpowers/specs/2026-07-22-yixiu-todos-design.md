# yixiu-todos (一修Todo) — 设计文档

> 跨平台桌面应用（Linux / Windows / macOS），用 **Rust + Tauri 2** 构建。
> 设计目标：多视图可扩展架构，让"我一天到底能不能做完这些事"一眼可见。

---

## 1. 项目信息

| 属性 | 值 |
|------|-----|
| 应用名 | `yixiu-todos` |
| 中文名 | 一修Todo |
| identifier | `com.yixiu.todos` |
| Rust crate | `yixiu_todos` / lib `yixiu_todos_lib` |
| 数据库文件 | `yixiu-todos.db` |
| 技术栈 | Rust + Tauri 2 + Vanilla TS（Vite 构建） |

---

## 2. 多视图架构

应用采用**多视图路由架构**，每个视图是一个独立的功能模块，通过前端路由切换：

```
┌─────────────────────────────────────────────────────────┐
│                   多视图路由层（前端）                    │
│  个人看板 │ 团队面板 │ 项目视图(TBD) │  更多(TBD)        │
└────────────────────┬────────────────────────────────────┘
                     │ Tauri invoke (IPC)
┌────────────────────▼────────────────────────────────────┐
│              Rust 后端模块化命令                         │
│  task.rs  conflict.rs  employee.rs  rules.rs ...        │
└─────────────────────────────────────────────────────────┘
```

### 2.1 个人看板（当前实现）

MVP 核心视图。功能包括：
- 个人待办 CRUD（标题/描述/优先级/起止时间/精力占用百分比）
- **精力占用面板**：按时间切片计算每一刻"同时在做"的任务精力之和
  - `≤ 100%` → 🟢 正常
  - `> 100%` → 🔴 红色告警（过载）
  - 单任务重叠 1 次（如两个 50% 任务撞车）→ 🟡 黄色警告
- 时间冲突自动检测，写入 `task_overlaps` 表，按严重程度排序
- 定时提醒（每 30 秒扫描，notify-rust 桌面通知）
- 强制录入规则
- 数据库维护（VACUUM / 导出 JSON/CSV）

### 2.2 团队面板（当前实现）

员工/团队管理视图。功能包括：
- 员工增删改查
- 给员工分配任务（独立 `employee_tasks` 表）
- 员工精力占用、平均进度、时间冲突告警计算

### 2.3 项目风险管理视图（TBD — 待开发）

> **状态**：列入路线图，尚未实现。架构预留。

未来可在此视图实现：
- 项目任务的风险等级登记（高/中/低）
- 影响面分析和缓解措施跟踪
- 风险 vs 进度看板

### 2.4 更多视图（TBD — 待开发）

> **状态**：预留扩展点。架构支持新增视图而无需修改后端核心逻辑。

---

## 3. 核心功能

### 3.0 STAR 任务定义体系

每个任务都按 **STAR 方法论** 分解为四个阶段，每个阶段可记录多条事件，并支持暂停/恢复：

| 字母 | 英文 | 中文 | 需要回答的问题 |
|------|------|------|--------------|
| S | Situation | 背景 | 当时是什么场景？约束是什么？ |
| T | Task | 任务 | 你被要求做什么？目标是什么？ |
| A | Action | 行动 | 你具体做了什么？（不是"我们"） |
| R | Result | 结果 | 产生了什么可衡量的结果？ |

#### 3.0.1 多事件与暂停

每个 STAR 阶段可以记录**多个事件**，事件的类型包括：

| 事件类型 | 含义 | 视觉效果 |
|---------|------|---------|
| `note` | 普通记录/更新 | 时间线节点 |
| `blocker` | 遇到阻碍 | 🔴 醒目色标 |
| `pause` | 工作暂停 | ⏸️ 暂停标记 |
| `resume` | 恢复工作 | ▶️ 恢复标记 |

> 例如：一个任务的 Action 阶段可能经历：写代码 → 遇到 bug(blocker) → 暂停(pause) → 恢复(resume) → 提交 PR，每个步骤都是一条独立事件记录。

#### 3.0.2 暂停跟踪

任务级别支持暂停状态，记录每次暂停的原因和时长：

- 暂停期间，精力占用面板不计该任务
- 冲突检测跳过暂停中的任务
- 前端显示累计暂停时长和暂停历史

#### 3.0.3 前端展示

每个任务的详情面板展示四个 STAR 区域（可折叠）：

```
┌─ 一修Todo ─────────────────────────────────┐
│ ┌─[S] 背景 ──────────────────────────┐      │
│ │ 当时是什么场景？约束是什么？         │      │
│ │ ○ 2026-07-20 接到需求，deadline 3天  │      │
│ │ ○ 2026-07-21 确认约束：只做后台API   │      │
│ └──────────────────────────────────────┘      │
│ ┌─[T] 任务 ──────────────────────────┐      │
│ │ 你被要求做什么？目标是什么？         │      │
│ │ ○ 实现用户认证模块                   │      │
│ └──────────────────────────────────────┘      │
│ ┌─[A] 行动 ──────────────────────────┐      │
│ │ 你具体做了什么？                    │      │
│ │ ○ 14:00 编写登录接口                │      │
│ │ 🔴 14:30 遇到数据库连接问题(blocker) │      │
│ │ ⏸️ 14:35 暂停(等待DBA回复)          │      │
│ │ ▶️ 16:20 恢复(问题已解决)           │      │
│ │ ○ 16:30 完成单元测试                │      │
│ └──────────────────────────────────────┘      │
│ ┌─[R] 结果 ──────────────────────────┐      │
│ │ 产生了什么可衡量的结果？            │      │
│ │ ○ 认证模块全部测试通过              │      │
│ └──────────────────────────────────────┘      │
│ [状态: 已完成]  [暂停 1次 共1h45min]  │      │
└──────────────────────────────────────────────┘
```

#### 3.0.4 扩展的 status

- `pending` / `active` / `done` / `postponed` / **`paused`**（新增）

### 3.1 精力占用面板

每个任务分配 `effort_percent`（0-200）。系统将一天按任务的起止时间切分，计算每个时间切片内的精力总和：

```
时间线:  09:00    10:00    11:00    12:00
任务A    ████████████████░░░░░░░░░░░░░░░  (50%)
任务B    ░░░░░░░░░░████████████░░░░░░░░░  (50%)
任务C    ░░░░░░░░░░░░░░░░░░░░████████████  (80%)
         🟢ok      🟢ok      🔴130%!!!
```

### 3.2 时间冲突自动检测

两两比较未完成任务的时间区间，计算交集分钟数。结果写入 `task_overlaps` 表：

- `warning`：精力占用 = 100%、或单次 50% 重叠（线程切换代价提示）
- `error`：精力占用 > 100%（必然有任务被挤掉）

### 3.3 强制录入规则

当检测到"受控软件"启动时，若用户未录入关联任务，每隔 N 分钟弹一次提醒。
事件全部留痕到 `force_events` 表。

### 3.4 定时提醒

后台调度器每 30 秒扫描即将开始的任务，通过 `notify-rust` 弹系统级桌面通知。
支持 `low / normal / critical` 三级紧急度。

---

## 4. 技术架构

### 4.1 后端（Rust + Tauri 2）

| 模块 | 职责 |
|------|------|
| `lib.rs` | Tauri 入口 + 命令注册 + 全局状态 |
| `db.rs` | SQLite 初始化 + 建表 + 连接管理 |
| `task.rs` | 个人任务 CRUD + 精力面板 + 导出 |
| `conflict.rs` | 时间冲突检测算法 |
| `scheduler.rs` | 后台 30s 调度器 |
| `reminder.rs` | 桌面通知 + 定时提醒调度 |
| `employee.rs` | 员工管理 + 任务分配 |
| `rules.rs` | 强制录入规则 |
| `tray.rs` | 系统托盘图标 + 菜单 |
| `error.rs` | 统一错误类型 |

### 4.2 前端（Vanilla TS，可迁移）

- 当前为 Vanilla TypeScript（Vite 构建）
- 可零成本迁移到 React/Vue + Tailwind CSS
- 前端通过 `@tauri-apps/api/core` 的 `invoke` 调用 Rust 命令

### 4.3 数据库（SQLite via rusqlite）

| 表名 | 作用 |
|------|------|
| `tasks` | 个人任务主表（status 扩充 `paused`） |
| `task_events` | STAR 事件日志（每行一个 S/T/A/R 事件） |
| `task_pauses` | 暂停记录（暂停 → 恢复配对，含原因和时长） |
| `task_overlaps` | 自动检测的冲突记录 |
| `reminders` | 提醒规则 |
| `force_rules` | 强制录入规则 |
| `force_events` | 强制规则触发事件日志 |
| `employees` | 员工主表 |
| `employee_tasks` | 分配给员工的任务 |

#### 新增表结构

```sql
-- STAR 事件日志
CREATE TABLE IF NOT EXISTS task_events (
    id            TEXT PRIMARY KEY,
    task_id       TEXT NOT NULL,
    star_section  TEXT NOT NULL CHECK(star_section IN ('S','T','A','R')),
    content       TEXT NOT NULL,
    event_type    TEXT NOT NULL DEFAULT 'note'
                  CHECK(event_type IN ('note','blocker','pause','resume')),
    created_at    TEXT NOT NULL,
    FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE
);

-- 暂停记录
CREATE TABLE IF NOT EXISTS task_pauses (
    id        INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id   TEXT NOT NULL,
    paused_at TEXT NOT NULL,
    resumed_at TEXT,          -- NULL = 仍在暂停中
    reason    TEXT,
    FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE
);
```

> `task_events` 中 `event_type='pause'` 触发同步写 `task_pauses`，
> `event_type='resume'` 触发更新对应 `task_pauses.resumed_at`。

---

## 5. 开发阶段

### 阶段 0.1（当前 — MVP）✅

- [x] 个人待办 CRUD + 精力占用面板
- [x] 时间冲突自动检测 (O(n²) 算法)
- [x] 定时提醒（notify-rust 桌面通知）
- [x] 员工/团队任务管理
- [x] 强制录入规则
- [x] 系统托盘
- [x] 数据库维护

### 阶段 0.2（近期规划）

- [ ] STAR 任务定义体系（`task_events` + `task_pauses` 表）
- [ ] 前端 STAR 面板（可折叠四段式 + 时间线事件展示）
- [ ] 暂停恢复功能、暂停统计
- [ ] 前端 UI 重构为多视图路由
- [ ] 精力面板可视化（甘特图/堆叠图）
- [ ] 前端迁移到 React/Vue + Tailwind

### 阶段 1.x（远期规划）

- [ ] 数据统计与趋势分析
- [ ] 提醒通道扩展（邮件/声音）
- [ ] 移动端适配（Tauri 2 iOS/Android）
- [ ] 更多待规划视图

---

## 6. 设计原则

- **模块化**：每个 Rust 模块一个清晰职责，通过 `lib.rs` 注册
- **松耦合**：新增视图无需修改后端核心逻辑
- **YAGNI**：当前只实现明确需要的功能，未来视图标记 TBD
- **数据驱动**：精力占用和冲突完全由数据计算，不依赖用户手动标注
